import { PayfromNamePipe } from './payfrom-name.pipe';

describe('PayfromNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PayfromNamePipe();
    expect(pipe).toBeTruthy();
  });
});
